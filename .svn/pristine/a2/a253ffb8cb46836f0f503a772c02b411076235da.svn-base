define(["lodash","package","lib/build-query"],function(b,a,c){var d="modernizr.com";if(typeof location!=="undefined"&&"host" in location){d=location.host}return function e(g,f){f=f||{};var h=c(f);if(!g||g==="compact"){return"/*! "+a.name+" "+a.version+" (Custom Build) | "+a.license+" *\n * http://"+d+"/download/"+h+" !*/\n"}else{if(g==="full"){return"/*!\n * "+a.name+" v"+a.version+"\n * Build http://"+d+"/download"+h+"\n *\n * Copyright (c)\n *  "+b.map(a.contributors,"name").join("\n *  ")+"\n\n * "+a.license+" License\n */\n\n/*\n * Modernizr tests which native CSS3 and HTML5 features are available in the\n * current UA and makes the results available to you in two ways: as properties on\n * a global `Modernizr` object, and as classes on the `<html>` element. This\n * information allows you to progressively enhance your pages with a granular level\n * of control over the experience.\n*/\n"}else{throw'banners() must be passed "compact" or "full" as an argument.'}}}});